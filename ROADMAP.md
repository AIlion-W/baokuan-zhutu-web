# ROADMAP · 爆款主图复刻 Web

> 记录这个 Web 工具现在做到哪、下一步做什么。行为改变后及时更新，避免 Agent 误判项目状态。

## 当前状态

项目已有第一版 Next.js 16 Web 实现，核心闭环是：

1. 上传一张爆款主图。
2. AI 按“场景 × 人群 × 需求”拆解。
3. 选择裂变方向：换场景、换需求、交叉重组。
4. 选择裂变张数。
5. 生成方案，输出中文结构化生图提示词，并调用生图模型。

API Key、Base URL、Claude 模型和生图模型由用户在前端设置弹窗中配置，保存在浏览器 `localStorage`。
用户可以编辑每个方案的生图提示词；如果方案已生成图片后又修改提示词，页面会提示需要重新生成，旧图保留作为上一次结果。

## 已锁定边界

- 不做账号体系。
- 不做数据库。
- 不把 API Key 写入服务端环境变量或仓库。
- 浏览器直连用户配置的中转站，不通过 Vercel API 中转层。
- 主目标是主图拆解与裂变，不是 CTR 排序或完整主图测评顾问。

## 下一步

1. 跑一次本地 `npm run lint` 和 `npm run build`，确认当前基线。
2. 用真实中转站 Key 做一次端到端冒烟：上传图 -> 拆解 -> 裂变 -> 生图。
3. 记录失败点：模型返回格式、图片上传体积、浏览器端状态恢复、错误提示。
4. 按真实使用反馈决定是否补：历史记录、方案导出、提示词模板版本管理。

## 验证命令

```bash
npm run lint
npm run build
```
